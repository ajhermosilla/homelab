# Environment files with secrets
.env

# Headscale config (contains private key reference)
networking/headscale/config/config.yaml
networking/headscale/config/private.key
networking/headscale/config/*.key

# Any local overrides
*.local
*.override
