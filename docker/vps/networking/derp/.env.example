# DERP Relay Environment Variables
# Copy to .env and customize

# Timezone
TZ=America/Asuncion

# DERP domain (must match DNS)
DERP_DOMAIN=derp.cronova.dev

# Verify clients via Tailscale API (false = open relay, true = requires Tailscale auth)
DERP_VERIFY_CLIENTS=false
