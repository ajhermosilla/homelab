- id: frigate_person_front_door
  alias: "Frigate: Person at Front Door"
  description: "Send ntfy notification when a person is detected in the driveway zone"
  trigger:
    - platform: mqtt
      topic: frigate/events
      payload: "new"
      value_template: "{{ value_json.type }}"
  condition:
    - condition: template
      value_template: >
        {{ trigger.payload_json.after.camera == 'front_door'
           and trigger.payload_json.after.label == 'person'
           and 'driveway' in trigger.payload_json.after.current_zones }}
  action:
    - service: notify.ntfy_homelab
      data:
        title: "Person in Driveway"
        message: "Person detected in driveway at {{ now().strftime('%H:%M') }}"

- id: frigate_person_back_yard
  alias: "Frigate: Person in Back Yard"
  description: "Send ntfy notification when a person is detected in the terraza zone"
  trigger:
    - platform: mqtt
      topic: frigate/events
      payload: "new"
      value_template: "{{ value_json.type }}"
  condition:
    - condition: template
      value_template: >
        {{ trigger.payload_json.after.camera == 'back_yard'
           and trigger.payload_json.after.label == 'person'
           and 'terraza' in trigger.payload_json.after.current_zones }}
  action:
    - service: notify.ntfy_homelab
      data:
        title: "Person in Terraza"
        message: "Person detected in terraza at {{ now().strftime('%H:%M') }}"

- id: frigate_person_indoor
  alias: "Frigate: Person Indoors (Away Mode)"
  description: "Send ntfy notification when a person is detected indoors"
  trigger:
    - platform: mqtt
      topic: frigate/events
      payload: "new"
      value_template: "{{ value_json.type }}"
  condition:
    - condition: template
      value_template: >
        {{ trigger.payload_json.after.camera == 'indoor'
           and trigger.payload_json.after.label == 'person' }}
  action:
    - service: notify.ntfy_homelab
      data:
        title: "Motion Indoors"
        message: "Person detected indoors at {{ now().strftime('%H:%M') }}"
