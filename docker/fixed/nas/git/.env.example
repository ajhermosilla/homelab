# NAS Git Stack Environment Variables
# Copy to .env and customize

# Forgejo data directory (SSD boot drive, not Purple)
FORGEJO_DATA=/srv/forgejo

# Root URL for Forgejo (used in links, clone URLs)
FORGEJO_ROOT_URL=https://git.cronova.dev
